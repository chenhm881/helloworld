<% if @comment.errors.empty? %>
  $('<%= j(render :partial => 'comment', :integer => @window, :object=> @comment) %>').appendTo($('#msg'));
<% else %>
  alert('@comment.errors.full_messages.join(',')');
<% end %>